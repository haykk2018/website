https://www.digitalocean.com/community/tutorials/how-to-install-mysql-on-ubuntu-18-04

my root password I set "password"

hayk@hayk-Aspire-E1-571:~$ mysql -u root -p


GRANT ALL PRIVILEGES ON *.* TO 'db_user'@'localhost' IDENTIFIED BY 'P@s$w0rd123!'; (Create a new database user)

SELECT user,authentication_string,plugin,host FROM mysql.user; (sure user is exist)

than create schema with intellij db admin

alter table <some_table> convert to character set utf8mb4 collate utf8mb4_unicode_ci (so can correct show data in other lang)
